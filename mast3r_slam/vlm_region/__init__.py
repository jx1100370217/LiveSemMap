"""VLM 区域生长语义地图 — 用 Qwen 的空间理解直接驱动区域切分。

与分水岭方案 (HMSG segment) 的根本区别: 区域边界由"空间身份连续性"定义
(每帧问 VLM: 4 环视所见是否仍是同一个物理空间), 而非墙体几何投影 ——
开放办公区/玻璃隔断等几何弱边界场景是它的优势。

流程 (在线增量):
  每关键帧 -> Qwen 判定 {是否仍在活动区域 / 是否回到某历史区域 / 当前空间
  身份描述+自身位置} -> 同空间: 帧与其点云 2D 足迹并入活动区域;
  空间变化: 关闭活动区域(触发总结命名) -> 回访则并入历史区域, 否则新建 +
  连接边 -> 区域足迹 = 成员帧点云 2D 投票网格 -> 楼层平面布局图。
"""
from .engine import VLMRegionEngine  # noqa: F401
